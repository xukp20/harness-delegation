{
  "name": "pi-agent-delegation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A self-contained Codex skill and Pi RPC task controller.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check scripts/pi-agent.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
