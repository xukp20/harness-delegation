interface:
  display_name: "Pi Delegation (Compatibility)"
  short_description: "Legacy Pi entry for Harness Delegation"
  default_prompt: "Use $pi-agent-delegation to delegate this bounded task to Pi and audit the receipt."

policy:
  allow_implicit_invocation: true
